import type {MetadataRoute} from "next";import {portfolio} from "@/data/portfolio";import {absoluteUrl} from "@/lib/seo";export default function sitemap():MetadataRoute.Sitemap{return[{url:absoluteUrl("/"),lastModified:new Date(),priority:1,changeFrequency:"monthly"},...portfolio.projects.map(p=>({url:absoluteUrl(`/projects/${p.slug}`),lastModified:new Date(),priority:.8,changeFrequency:"monthly" as const}))]}