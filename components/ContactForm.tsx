"use client";
import {FormEvent,useState} from "react";import {Loader2,Send,CheckCircle2,AlertCircle} from "lucide-react";
export function ContactForm(){const[state,setState]=useState("idle");const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const form=e.currentTarget;const body=Object.fromEntries(new FormData(form).entries());try{const r=await fetch("/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.message);setState("success");setMsg(d.message);form.reset()}catch(err){setState("error");setMsg(err instanceof Error?err.message:"Something went wrong.")}}return <form onSubmit={submit} className="space-y-5"><label className="absolute -left-[9999px] h-px w-px overflow-hidden">Website<input name="website" tabIndex={-1} autoComplete="off"/></label><div className="grid gap-5 sm:grid-cols-2"><input name="name" required minLength={2} placeholder="Your name" className="w-full rounded-2xl border border-black/10 bg-transparent px-4 py-3 outline-none dark:border-white/10"/><input name="email" required type="email" placeholder="you@example.com" className="w-full rounded-2xl border border-black/10 bg-transparent px-4 py-3 outline-none dark:border-white/10"/></div><input name="subject" required minLength={3} placeholder="Subject" className="w-full rounded-2xl border border-black/10 bg-transparent px-4 py-3 outline-none dark:border-white/10"/><textarea name="message" required minLength={10} rows={6} placeholder="Tell me about your project..." className="w-full resize-y rounded-2xl border border-black/10 bg-transparent px-4 py-3 outline-none dark:border-white/10"/><button disabled={state==="sending"} className="inline-flex items-center gap-2 rounded-full bg-black px-6 py-3.5 text-sm font-semibold text-white disabled:opacity-60 dark:bg-white dark:text-black">{state==="sending"?<Loader2 size={16} className="animate-spin"/>:<Send size={16}/>} {state==="sending"?"Sending...":"Send Message"}</button>{msg&&<div role="status" className="flex gap-2 rounded-2xl bg-neutral-100 p-4 text-sm dark:bg-neutral-900">{state==="success"?<CheckCircle2 size={17}/>:<AlertCircle size={17}/>} {msg}</div>}</form>}