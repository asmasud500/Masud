type Entry={count:number;resetAt:number};const store=new Map<string,Entry>();export function rateLimit(key:string,limit=5,windowMs=600000){const now=Date.now(),cur=store.get(key);if(!cur||cur.resetAt<=now){store.set(key,{count:1,resetAt:now+windowMs});return{success:true,remaining:limit-1}}if(cur.count>=limit)return{success:false,remaining:0,retryAfter:Math.ceil((cur.resetAt-now)/1000)};cur.count++;return{success:true,remaining:limit-cur.count}}