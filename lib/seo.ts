import type {Metadata} from "next";import {portfolio} from "@/data/portfolio";const site=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";export function absoluteUrl(path="/"){return new URL(path,site).toString()}export function buildMetadata():Metadata{return{metadataBase:new URL(site),title:{default:`${portfolio.personal.name} — ${portfolio.personal.heroTitle}`,template:`%s | ${portfolio.personal.name}`},description:portfolio.personal.intro,alternates:{canonical:"/"},openGraph:{type:"website",url:site,title:`${portfolio.personal.name} — ${portfolio.personal.heroTitle}`,description:portfolio.personal.intro,images:[absoluteUrl("/og-image.svg")]},twitter:{card:"summary_large_image",title:portfolio.personal.name,description:portfolio.personal.intro,images:[absoluteUrl("/og-image.svg")]},robots:{index:true,follow:true}}}